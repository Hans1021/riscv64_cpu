# Definition of Done

- Every milestone must have:
  - A command to run it
  - Automated pass/fail
  - A doc note listing what was added
- Every bug fix must include:
  - A regression test or an existing test that now catches it
- Every commit must:
  - Keep CI green
